# Localization server interfaces

Here we define the interfaces of our localization server coordinate retreiver.

MyServiceMessage.srv contains the definition of our service message to be used in project_localization package.

We can use `ros2 service list` to ensure the service is ready to use.

